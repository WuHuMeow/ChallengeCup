# 交付 A（DA） W4 任务书

> 周期：8/10（周日）- 8/16（周六） | 核心目标：完成报告第四/五/六章、更新 PPT 全部数据页

---

## 每日任务

### Day 1（8/10 周日）

- [ ] 根据 TL review 意见修改报告第一/二/三章（最终修改）
- [ ] 补充 3.5 节 EWMA 预测的原理说明（W4 算法已实现，补充实现细节）
- [ ] 写入 EWMA 公式和参数选择依据
- [ ] 确认前三章修改后与第四章数据引用一致

EWMA 预测写作模板：

```
3.5 改进 4：EWMA 流量预测
    公式：predicted(t) = α × current(t) + (1-α) × predicted(t-1)
    融入方式：adjusted_pressure = (queue + predicted) / capacity
    参数选择：α = 0.3（平衡响应速度与平滑度）
    设计意图：在压力计算中引入未来流量估计，
              使算法在流量上升期提前切换相位，减少被动响应延迟。
```

**验证：** 打开报告 3.5 节，确认：① 含 EWMA 公式（编号正确）；② 含融入方式公式；③ 含参数 α 的选择依据；④ 前三章无遗留 review 意见。

### Day 2（8/11 周一）

- [ ] 撰写报告 5.1 节"标准化算法接口"：BaseControlAlgorithm 设计、插件化机制
- [ ] 撰写报告 5.2 节"Docker 容器化部署"：引用 IA 的 Dockerfile，说明一键运行
- [ ] 撰写报告 5.3 节"可复现性"：一键运行命令、随机种子控制、环境锁定
- [ ] 向 IA 获取 Dockerfile 和 deployment.md 素材

第五章写作模板：

```
5.1 标准化算法接口
    - BaseControlAlgorithm 抽象类：step(joint_state) -> control_action
    - 插件化：新算法只需继承并实现 step()，无需修改框架代码
    - 已实现：FixedTime、Actuated、CA-MP 三个插件

5.2 Docker 容器化部署
    - 基础镜像：python:3.10-slim + SUMO
    - 一键运行：docker compose up
    - 环境锁定：requirements.txt 固定版本

5.3 可复现性说明
    - 随机种子：--seed 42/123/456
    - 一键复现命令：python run_experiment.py --all
    - 结果输出：output/csv/（360 个 CSV 文件）
```

**验证：** 打开报告第五章，确认：① 3 个小节有正文；② 含 BaseControlAlgorithm 接口说明；③ 含 Docker 命令示例；④ 含可复现命令和种子说明。

### Day 3（8/12 周二）

- [ ] 收到 EX 的 1.5 倍压力数据后，更新报告 4.5 节"1.5 倍压力测试结果"
- [ ] 撰写分析："高压力下 CA-MP 优势更明显"（溢出门控 + 容量归一化在高压下作用更大）
- [ ] 制作 1.5 倍压力对比表（标准流量 vs 高压流量，改进幅度对比）
- [ ] 更新 PPT 第 14-15 页：加入 1.5 倍压力对比数据

**验证：** 打开报告 4.5 节，确认：① 含 1.5 倍压力数据表（20 路口或重点路口）；② 分析文字解释了"为什么高压下优势更大"；③ PPT 第 14-15 页含高压数据。

### Day 4（8/13 周三）

- [ ] 收到 EX 的参数敏感性数据后，在报告第四章添加"参数敏感性分析"小节
- [ ] 插入敏感性曲线图（DB 产出）：α 值 vs 各指标
- [ ] 说明最优参数选择依据（α=0.3 的选取理由）
- [ ] 更新 PPT 相关页面

**验证：** 打开报告参数敏感性小节，确认：① 含 ≥ 1 张敏感性曲线图；② 说明了 α 的搜索范围和最优值；③ 图表编号正确、引用完整。

### Day 5（8/14 周四）

- [ ] 撰写报告 6.1 节"创新点总结"：3 个核心创新 + EWMA（对应评分标准"创新性"10 分）
- [ ] 撰写报告 6.2 节"局限性分析"：单路口优化、未考虑行人/非机动车
- [ ] 撰写报告 6.3 节"未来工作"：CMP 多路口协同（GAT+TCN）、完整 PPO 绿灯优化
- [ ] 更新 PPT 第 18 页（创新点 + 展望）

创新点总结模板：

```
6.1 创新点总结
    创新 1：容量归一化压力 —— 解决短边/长边竞争不公平（对应雄安 24m 短边场景）
    创新 2：溢出门控机制 —— 占用率 > 90% 强制放行，防止短边锁死
    创新 3：云端动态绿灯时长 —— CloudPolicy 根据全局流量动态调整
    辅助创新：EWMA 流量预测 —— 前瞻性压力估计，减少被动响应

6.2 局限性
    - 当前为单路口独立优化，路口间协同依赖云端策略
    - 未建模行人、非机动车交通流
    - EWMA 对突变流量响应滞后

6.3 未来工作
    - CMP 多路口协同：GAT 建模路口图 + TCN 时序预测
    - 完整 PPO 绿灯优化：端到端强化学习
    - 多模态交通融合
```

**验证：** 打开报告第六章，确认：① 6.1 含 3+1 个创新点，每个有 1-2 句说明；② 6.2 含 ≥ 2 条局限性；③ 6.3 含 ≥ 2 个未来方向；④ PPT 第 18 页已更新。

### Day 6（8/15 周五）

- [ ] 报告全文通读：检查逻辑连贯性（章节间过渡自然）
- [ ] 检查图表编号连续、正文引用正确
- [ ] 检查错别字、标点、格式（字体、行距、页边距）
- [ ] 提交给 TL 做最终 review

**验证：** 全文通读 checklist：① 图表编号 1-1 至 6-N 连续无跳号；② 每处"图/表 X-X"引用对应正确；③ 无错别字（可用 Word 拼写检查辅助）；④ TL 确认收到全文。

### Day 7（8/16 周六）

- [ ] 修改 TL review 意见
- [ ] PPT 全文通读：确保 19 页内容与报告一致
- [ ] 准备 W5 的视频旁白文字稿框架（与 DB 协调分工）

**验证：** ① TL review 意见全部处理；② PPT 19 页逐页与报告对应章节核对无矛盾；③ 旁白框架已发给 DB 确认。

---

## 交付物

| # | 文件 | 截止日 | 验收标准 |
|---|------|--------|----------|
| 1 | 报告第五章 | 8/11 | 3 小节完整，含接口说明 + Docker 命令 + 复现步骤 |
| 2 | 报告 4.5 节更新（1.5 倍压力） | 8/12 | 含高压数据表 + 分析文字 |
| 3 | 报告参数敏感性小节 | 8/13 | 含敏感性曲线图 + 参数选择依据 |
| 4 | 报告第六章 | 8/14 | 3+1 创新点 + 局限性 + 未来工作 |
| 5 | 报告全文通读版 | 8/15 | 无格式错误、图表编号连续、引用正确 |
| 6 | PPT 全部数据页更新 | 8/15 | 19 页内容与报告一致 |

## 协作对接

- 与 EX 对接：获取 1.5 倍压力数据（Day 3）和参数敏感性数据（Day 4）
- 与 IA 对接：获取 Dockerfile、deployment.md（Day 2）
- 与 DB 对接：获取敏感性曲线图（Day 4）、协调视频旁白分工（Day 7）
